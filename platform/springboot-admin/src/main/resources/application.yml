spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:cdc_warehouse_admin}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  freemarker:
    charset: UTF-8
    suffix: .ftl
    template-loader-path: classpath:/templates/

server:
  port: 8080

warehouse:
  project-root: ../..
  mysql:
    fallback-demo-data: true
  auth:
    admin-user: ${ADMIN_USER:admin}
    admin-pass: ${ADMIN_PASS:admin123}
    jwt-secret: ${JWT_SECRET:cdc-warehouse-platform-secret-key-2026-minimum-256-bit-length}
    jwt-expiration-ms: 86400000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
